# AI collaboration entry

> Before working, read `PROJECT.md` and maintain its single current step. Put detailed knowledge in the linked `docs/` file instead of duplicating project tasks.

- Update `docs/requirements.md` when product scope changes.
- Update `docs/design.md` when interface behavior changes.
- Update the frontend, backend, API, or architecture guide when implementation constraints change.
- After validation, update `docs/testing.md` and the current round in `PROJECT.md`.
