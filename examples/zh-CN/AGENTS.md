# AI 协作入口

> AI 工具开始工作前先读取 `PROJECT.md`，只维护其中唯一的当前步骤；详情写入对应 `docs/` 文件，不复制项目任务。

- 需求变化更新 `docs/requirements.md`。
- 界面与交互变化更新 `docs/design.md`。
- 实现约束分别更新前端、后端、API 或架构文档。
- 验证完成后更新 `docs/testing.md` 和 `PROJECT.md` 的本轮进度。
